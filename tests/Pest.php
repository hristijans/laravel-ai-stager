<?php

// Each test file declares its own TestCase via uses() because different
// tests require different boot configurations (stager enabled vs disabled).
//
// Available TestCases:
//   TestCase              — stager disabled (default)
//   StagerEnabledTestCase — stager enabled at boot (AI_STAGER_ENABLED=true)
